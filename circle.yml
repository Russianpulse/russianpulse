machine:
  services:
    - docker

dependencies:
  pre:
    - pip install --upgrade pip
    - pip install docker-compose

test:
  pre:
    - docker-compose pull
  override:
    - docker-compose -f docker-compose.yml run sut bundle exec rake db:create db:migrate spec
