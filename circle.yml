machine:
  ruby:
    version: ruby-2.3.1

test:
  pre:
    - bundle exec bundle-audit check --update

deployment:
  hub:
    branch: master
    commands:
      - ssh deployer@$DEPLOY_HOST "cd mazavr && git pull && make run"
